{
  "name": "begin-app",
  "version": "0.0.2",
  "description": "Begin basic Hello World! app for Node",
  "scripts": {
    "start": "cross-env NODE_ENV=testing npx sandbox"
  },
  "devDependencies": {
    "@architect/sandbox": "latest",
    "cross-env": "^6.0.3"
  },
  "dependencies": {
    "@octokit/oauth-app": "^1.0.1"
  }
}